doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Pratistha Financial Services
    
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css")
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
    link(href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Outfit:wght@300;400;600&display=swap" rel="stylesheet")
    
    style.
      :root {
        --primary-emerald: #059669;
        --primary-dark: #047857;
        --accent-gold: #f59e0b;
        --text-primary: #1f2937;
        --text-secondary: #6b7280;
        --bg-light: #f9fafb;
        --gradient-start: #10b981;
        --gradient-end: #059669;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Outfit', sans-serif;
        color: var(--text-primary);
        overflow-x: hidden;
      }

      h1, h2, h3, h4, h5 {
        font-family: 'Playfair Display', serif;
      }

      .navbar {
        background: linear-gradient(135deg, #1f2937 0%, #111827 100%) !important;
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
      }

      .navbar.scrolled {
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
      }

      .navbar-brand {
        font-weight: 700;
        font-size: 1.4rem;
        letter-spacing: -0.5px;
      }

      .navbar-brand img {
        border: 2px solid var(--accent-gold);
        transition: transform 0.3s ease;
      }

      .navbar-brand:hover img {
        transform: rotate(360deg) scale(1.1);
      }

      .nav-link {
        position: relative;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .nav-link::after {
        content: '';
        position: absolute;
        bottom: -5px;
        left: 50%;
        width: 0;
        height: 2px;
        background: var(--accent-gold);
        transition: all 0.3s ease;
        transform: translateX(-50%);
      }

      .nav-link:hover::after {
        width: 80%;
      }

      .hero {
        background: linear-gradient(135deg, rgba(5, 150, 105, 0.95) 0%, rgba(4, 120, 87, 0.9) 100%), url('./img/bd.jpg') center/cover no-repeat;
        padding: 180px 20px 120px;
        position: relative;
        overflow: hidden;
      }

      .hero::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: radial-gradient(circle at 30% 50%, rgba(245, 158, 11, 0.15) 0%, transparent 50%);
        animation: pulse 8s ease-in-out infinite;
      }

      @keyframes pulse {
        0%, 100% { opacity: 0.5; }
        50% { opacity: 1; }
      }

      .hero-overlay {
        background: rgba(255, 255, 255, 0.98);
        padding: 60px;
        border-radius: 30px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
        position: relative;
        z-index: 1;
        backdrop-filter: blur(10px);
        animation: slideUp 0.8s ease-out;
      }

      @keyframes slideUp {
        from {
          opacity: 0;
          transform: translateY(50px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .hero h1 {
        font-size: clamp(2rem, 5vw, 3.5rem);
        background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 1rem;
      }

      .hero .lead {
        font-size: clamp(1rem, 2vw, 1.25rem);
        color: var(--text-secondary);
      }

      .btn-modern {
        padding: 14px 35px;
        border-radius: 50px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.3s ease;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        font-size: 0.9rem;
      }

      .btn-modern:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      }

      .btn-gradient {
        background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
        border: none;
        color: white;
      }

      .btn-gradient:hover {
        background: linear-gradient(135deg, var(--gradient-end), var(--primary-dark));
        color: white;
      }

      .features-section {
        padding: 100px 0;
        background: linear-gradient(180deg, #ffffff 0%, var(--bg-light) 100%);
      }

      .section-title {
        font-size: clamp(2rem, 4vw, 3rem);
        margin-bottom: 3rem;
        position: relative;
        display: inline-block;
      }

      .section-title::after {
        content: '';
        position: absolute;
        bottom: -15px;
        left: 50%;
        transform: translateX(-50%);
        width: 100px;
        height: 4px;
        background: linear-gradient(90deg, var(--accent-gold), var(--primary-emerald));
        border-radius: 2px;
      }

      .feature-card {
        text-align: center;
        padding: 30px;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        border-radius: 20px;
        height: 100%;
      }

      .feature-card:hover {
        transform: translateY(-10px);
        background: white;
        box-shadow: 0 15px 40px rgba(5, 150, 105, 0.15);
      }

      .feature-icon-wrapper {
        width: 120px;
        height: 120px;
        margin: 0 auto 1.5rem;
        position: relative;
        border-radius: 50%;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      }

      .feature-icon-wrapper::before {
        content: '';
        position: absolute;
        inset: -3px;
        background: linear-gradient(135deg, var(--primary-emerald), var(--accent-gold));
        border-radius: 50%;
        z-index: -1;
        animation: rotate 3s linear infinite;
      }

      @keyframes rotate {
        100% { transform: rotate(360deg); }
      }

      .feature-icon {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 50%;
      }

      .feature-card h5 {
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--text-primary);
      }

      .reviews-section {
        padding: 100px 0;
        background: var(--bg-light);
      }

      .review-card {
        background: white;
        border-radius: 25px;
        padding: 40px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
        transition: all 0.3s ease;
        margin-bottom: 30px;
        border: 1px solid rgba(5, 150, 105, 0.1);
      }

      .review-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 50px rgba(5, 150, 105, 0.15);
      }

      .review-avatar {
        width: 90px;
        height: 90px;
        border-radius: 50%;
        border: 4px solid var(--primary-emerald);
        margin-bottom: 1.5rem;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .review-text {
        font-style: italic;
        color: var(--text-secondary);
        font-size: 1.1rem;
        line-height: 1.8;
        margin-bottom: 1rem;
      }

      .review-author {
        color: var(--primary-emerald);
        font-weight: 600;
        font-size: 1rem;
      }

      .faq-section {
        padding: 100px 0;
        background: white;
      }

      .accordion-item {
        border: none;
        margin-bottom: 15px;
        border-radius: 15px !important;
        overflow: hidden;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
      }

      .accordion-button {
        background: var(--bg-light);
        font-weight: 600;
        font-size: 1.05rem;
        padding: 1.5rem;
        color: var(--text-primary);
        border-radius: 15px !important;
      }

      .accordion-button:not(.collapsed) {
        background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
        color: white;
        box-shadow: none;
      }

      .accordion-button:focus {
        box-shadow: none;
        border: none;
      }

      .accordion-body {
        padding: 1.5rem;
        color: var(--text-secondary);
        font-size: 1rem;
        line-height: 1.7;
      }

      .contact-section {
        padding: 100px 0;
        background: linear-gradient(135deg, var(--bg-light) 0%, white 100%);
      }

      .form-control, .form-select {
        padding: 15px 20px;
        border-radius: 12px;
        border: 2px solid #e5e7eb;
        font-size: 1rem;
        transition: all 0.3s ease;
      }

      .form-control:focus, .form-select:focus {
        border-color: var(--primary-emerald);
        box-shadow: 0 0 0 0.2rem rgba(5, 150, 105, 0.15);
      }

      .input-group {
        position: relative;
      }

      .phone-input-wrapper {
        display: flex;
        gap: 10px;
      }

      .country-code-select {
        flex: 0 0 120px;
      }

      .phone-input {
        flex: 1;
      }

      .form-label {
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
      }

      .invalid-feedback, .valid-feedback {
        font-size: 0.875rem;
        margin-top: 0.5rem;
      }

      .char-count {
        font-size: 0.85rem;
        color: var(--text-secondary);
        text-align: right;
        margin-top: 0.25rem;
      }

      .char-count.warning {
        color: #f59e0b;
      }

      .char-count.danger {
        color: #ef4444;
      }

      footer {
        background: linear-gradient(135deg, #1f2937 0%, #111827 100%);
        color: white;
        padding: 40px 0 20px;
      }

      .footer-content {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        gap: 30px;
        margin-bottom: 30px;
      }

      .footer-section {
        flex: 1;
        min-width: 250px;
      }

      .footer-section h5 {
        font-size: 1.2rem;
        margin-bottom: 1rem;
        color: var(--accent-gold);
      }

      .footer-links {
        list-style: none;
        padding: 0;
      }

      .footer-links li {
        margin-bottom: 0.5rem;
      }

      .footer-links a {
        color: #d1d5db;
        text-decoration: none;
        transition: color 0.3s ease;
      }

      .footer-links a:hover {
        color: var(--accent-gold);
      }

      .social-icons a {
        color: white;
        font-size: 1.5rem;
        margin-right: 15px;
        transition: all 0.3s ease;
      }

      .social-icons a:hover {
        color: var(--accent-gold);
        transform: translateY(-3px);
      }

      .copyright {
        text-align: center;
        padding-top: 20px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        color: #9ca3af;
      }

      @media (max-width: 770px) {
        .hero {
          padding: 140px 15px 80px;
        }

        .hero-overlay {
          padding: 40px 25px;
          border-radius: 20px;
        }

        .features-section, .reviews-section, .faq-section, .contact-section {
          padding: 60px 0;
        }

        .section-title {
          font-size: 2rem;
        }

        .btn-modern {
          padding: 12px 25px;
          font-size: 0.85rem;
        }

        .feature-icon-wrapper {
          width: 100px;
          height: 100px;
        }

        .review-card {
          padding: 30px 20px;
        }

        .phone-input-wrapper {
          flex-direction: column;
        }

        .country-code-select {
          flex: 1;
        }

        .features-section .col-lg-4 {
          flex: 0 0 50%;
          max-width: 50%;
        }
      }

      @media (max-width: 576px) {
        .navbar-brand {
          font-size: 1.1rem;
        }

        .hero h1 {
          font-size: 1.75rem;
        }

        .hero .lead {
          font-size: 0.95rem;
        }

        .d-flex.gap-3 {
          flex-direction: column;
          width: 100%;
        }

        .btn-modern {
          width: 100%;
        }
      }

      .fade-in {
        animation: fadeIn 0.6s ease-in;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .spinner-border-sm {
        width: 1rem;
        height: 1rem;
        border-width: 0.15em;
      }

  body
    //- ================= NAVBAR =================
    nav.navbar.navbar-expand-lg.navbar-dark.fixed-top
      .container
        a.navbar-brand.d-flex.align-items-center(href="/")
          img.rounded-circle(src="./img/nlogo.png" width="45" alt="Logo")
          span.ms-2 Pratistha Financial Services

        button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navMenu")
          span.navbar-toggler-icon

        #navMenu.collapse.navbar-collapse
          ul.navbar-nav.ms-auto
            li.nav-item
              a.nav-link(href="/") Home
            li.nav-item
              a.nav-link(href="/services") Services
            li.nav-item
              a.nav-link(href="/about") About
            li.nav-item
              a.nav-link(href="/contact") Contact
            //- li.nav-item
            //-   a.nav-link.text-warning(href="/login")
            //-     i.fas.fa-user.me-1
            //-     | User Login

    //- ================= HERO =================
    section.hero
      .container
        .hero-overlay.text-center
          h1.fw-bold Pratistha Financial Services
          p.lead
            | We assure complete cooperation to fulfill your financial needs.
          .d-flex.justify-content-center.gap-3.mt-4.flex-wrap
            a.btn.btn-gradient.btn-modern(href="/applyloan")
              i.fas.fa-file-invoice-dollar.me-2
              | Apply Loan
            a.btn.btn-outline-primary.btn-modern(href="/EMI")
              i.fas.fa-calculator.me-2
              | EMI Calculator

    //- ================= FEATURES =================
    section.features-section
      .container
        h2.text-center.section-title Why Choose Us
        .row.g-4
          .col-md-6.col-lg-4
            .feature-card.fade-in
              .feature-icon-wrapper
                img.feature-icon(src="./img/cop.jpg" alt="100% Cooperation")
              h5 100% Cooperation
          .col-md-6.col-lg-4
            .feature-card.fade-in(style="animation-delay: 0.1s;")
              .feature-icon-wrapper
                img.feature-icon(src="./img/time.jpg" alt="Fast Processing")
              h5 Fast Processing
          .col-md-6.col-lg-4
            .feature-card.fade-in(style="animation-delay: 0.2s;")
              .feature-icon-wrapper
                img.feature-icon(src="./img/max.jpg" alt="Maximum Amount")
              h5 Maximum Amount
          .col-md-6.col-lg-4
            .feature-card.fade-in(style="animation-delay: 0.3s;")
              .feature-icon-wrapper
                img.feature-icon(src="./img/intrest.jpg" alt="Minimum Interest")
              h5 Minimum Interest
          .col-md-6.col-lg-4
            .feature-card.fade-in(style="animation-delay: 0.4s;")
              .feature-icon-wrapper
                img.feature-icon(src="./img/repay.jpg" alt="Flexible Repayment")
              h5 Flexible Repayment
          .col-md-6.col-lg-4
            .feature-card.fade-in(style="animation-delay: 0.5s;")
              .feature-icon-wrapper
                img.feature-icon(src="./img/suces.jpg" alt="Sure Success")
              h5 Sure Success

    //- ================= USER REVIEWS =================
    section.reviews-section
      .container
        h2.text-center.section-title.mb-5 Client Reviews

        .row.justify-content-center
          .col-lg-8
            .review-card.text-center
              p.review-text
                | "I love you the more in that I believe you had liked me for my own sake and for nothing else."
              p.review-author — John Keats

            .review-card.text-center
              p.review-text
                | "A man can be destroyed but not defeated."
              p.review-author — Ernest Hemingway

            .review-card.text-center
              p.review-text
                | "I have not failed. I've just found 10,000 ways that won't work."
              p.review-author — Thomas Edison

    //- ================= FAQ =================
    section.faq-section
      .container
        h2.text-center.section-title.mb-5 Frequently Asked Questions

        .accordion#faqAccordion
          .accordion-item
            h2.accordion-header
              button.accordion-button(type="button" data-bs-toggle="collapse" data-bs-target="#f1")
                | How much amount you can manage for borrower?
            #f1.accordion-collapse.collapse.show(data-bs-parent="#faqAccordion")
              .accordion-body
                | Loan range: ₹30,000 – ₹500 Crore (T&C apply)

          .accordion-item
            h2.accordion-header
              button.accordion-button.collapsed(type="button" data-bs-toggle="collapse" data-bs-target="#f2")
                | What type of financers you have?
            #f2.accordion-collapse.collapse(data-bs-parent="#faqAccordion")
              .accordion-body
                | Govt & Pvt Banks, NBFCs, Private lenders

          .accordion-item
            h2.accordion-header
              button.accordion-button.collapsed(type="button" data-bs-toggle="collapse" data-bs-target="#f3")
                | How long does loan disbursal take?
            #f3.accordion-collapse.collapse(data-bs-parent="#faqAccordion")
              .accordion-body
                | Maximum 3 working days if documents are correct

          .accordion-item
            h2.accordion-header
              button.accordion-button.collapsed(type="button" data-bs-toggle="collapse" data-bs-target="#f4")
                | Loan sanction and disbursal same?
            #f4.accordion-collapse.collapse(data-bs-parent="#faqAccordion")
              .accordion-body
                | Sanction is approval, disbursal is actual fund transfer

          .accordion-item
            h2.accordion-header
              button.accordion-button.collapsed(type="button" data-bs-toggle="collapse" data-bs-target="#f5")
                | Cheque bounce customer eligible?
            #f5.accordion-collapse.collapse(data-bs-parent="#faqAccordion")
              .accordion-body
                | Yes, with additional conditions (T&C apply)

          .accordion-item
            h2.accordion-header
              button.accordion-button.collapsed(type="button" data-bs-toggle="collapse" data-bs-target="#f6")
                | Low CIBIL but property available?
            #f6.accordion-collapse.collapse(data-bs-parent="#faqAccordion")
              .accordion-body
                | Yes, please contact customer support

    //- ================= FOOTER =================
    footer
      .container
        .footer-content
          .footer-section
            h5 Pratistha Financial Services
            p(style="color: #d1d5db;")
              | Your trusted partner for all financial solutions. We provide comprehensive loan services with complete transparency and support.
            //- .social-icons.mt-3
            //-   a(href="#")
            //-     i.fab.fa-facebook
            //-   a(href="#")
            //-     i.fab.fa-twitter
            //-   a(href="#")
            //-     i.fab.fa-linkedin
            //-   a(href="#")
            //-     i.fab.fa-instagram
          
          .footer-section
            h5 Quick Links
            ul.footer-links
              li
                a(href="/") Home
              li
                a(href="/services") Services
              li
                a(href="/about") About Us
              li
                a(href="/contact") Contact
              li
                a(href="/applyloan") Apply for Loan
          
          .footer-section
            h5 Contact Info
            ul.footer-links
              //- li
              //-   i.fas.fa-phone.me-2
              //-   | +91 77039 21037
              //- li
              //-   i.fas.fa-envelope.me-2
              //-   | info@pratistha.com
              li
                i.fas.fa-map-marker-alt.me-2
                | Delhi, India
              li
                i.fas.fa-clock.me-2
                | Mon-Sat: 9AM - 6PM
        
        .copyright
          p
            | © 2026 Pratistha Financial Services | Site Under Development 
            //- a(href="/privacy" style="color: #9ca3af; text-decoration: none;") Privacy Policy
            //- |  | 
            //- a(href="/terms" style="color: #9ca3af; text-decoration: none;") Terms & Conditions

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js")
    
    script.
      window.addEventListener('scroll', function() {
        const navbar = document.querySelector('.navbar');
        if (window.scrollY > 50) {
          navbar.classList.add('scrolled');
        } else {
          navbar.classList.remove('scrolled');
        }
      });

      const form = document.getElementById('contactForm');
      const phoneInput = document.getElementById('phone');
      const messageInput = document.getElementById('message');
      const charCount = document.getElementById('charCount');
      const phoneError = document.getElementById('phoneError');
      const phoneSuccess = document.getElementById('phoneSuccess');

      messageInput.addEventListener('input', function() {
        const length = this.value.length;
        charCount.textContent = `${length} / 500 characters`;
        
        if (length > 450) {
          charCount.classList.add('danger');
          charCount.classList.remove('warning');
        } else if (length > 400) {
          charCount.classList.add('warning');
          charCount.classList.remove('danger');
        } else {
          charCount.classList.remove('warning', 'danger');
        }
      });

      phoneInput.addEventListener('input', function() {
        this.value = this.value.replace(/[^0-9]/g, '');
        
        if (this.value.length > 10) {
          this.value = this.value.slice(0, 10);
        }

        if (this.value.length === 10) {
          this.classList.remove('is-invalid');
          this.classList.add('is-valid');
          phoneError.textContent = '';
          phoneSuccess.textContent = 'Valid mobile number';
          phoneSuccess.style.display = 'block';
          phoneError.style.display = 'none';
        } else if (this.value.length > 0) {
          this.classList.remove('is-valid');
          this.classList.add('is-invalid');
          phoneError.textContent = 'Mobile number must be exactly 10 digits';
          phoneError.style.display = 'block';
          phoneSuccess.style.display = 'none';
        } else {
          this.classList.remove('is-valid', 'is-invalid');
          phoneError.style.display = 'none';
          phoneSuccess.style.display = 'none';
        }
      });

      form.addEventListener('submit', function(event) {
        event.preventDefault();
        event.stopPropagation();

        if (form.checkValidity()) {
          const submitBtn = document.getElementById('submitBtn');
          const submitText = document.getElementById('submitText');
          const submitSpinner = document.getElementById('submitSpinner');
          
          submitBtn.disabled = true;
          submitText.classList.add('d-none');
          submitSpinner.classList.remove('d-none');

          const formData = {
            name: document.getElementById('name').value,
            email: document.getElementById('email').value,
            countryCode: document.getElementById('countryCode').value,
            phone: document.getElementById('phone').value,
            state: document.getElementById('state').value,
            pincode: document.getElementById('pincode').value,
            message: document.getElementById('message').value
          };

          setTimeout(() => {
            console.log('Form submitted:', formData);
            
            form.reset();
            form.classList.remove('was-validated');
            
            submitBtn.disabled = false;
            submitText.classList.remove('d-none');
            submitSpinner.classList.add('d-none');
            
            charCount.textContent = '0 / 500 characters';
            charCount.classList.remove('warning', 'danger');
            
            alert('Thank you! Your request has been submitted successfully. We will contact you soon.');
          }, 2000);
        }

        form.classList.add('was-validated');
      });

      const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
      };

      const observer = new IntersectionObserver(function(entries) {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
          }
        });
      }, observerOptions);

      document.querySelectorAll('.fade-in').forEach(el => {
        el.style.opacity = '0';
        el.style.transform = 'translateY(20px)';
        el.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
        observer.observe(el);
      });